# action-pnpm-install (deprecated)

This repository is **deprecated**. The composite action has been migrated to [emo-workflows](https://github.com/emocional/emo-workflows).

Use instead:

```yaml
uses: emocional/emo-workflows/.github/actions/pnpm-install@main
```

Source: `.github/actions/pnpm-install/action.yml` in `emo-workflows`.
